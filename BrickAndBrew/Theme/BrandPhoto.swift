import Foundation

/// Catalog names for editorial brand photography. Crop in the view, not the asset.
enum BrandPhoto {
    static let welcomeHero = "PhotoWelcomeHero"
    static let nightCheers = "PhotoNightCheers"
    static let tableSession = "PhotoTableSession"
}
