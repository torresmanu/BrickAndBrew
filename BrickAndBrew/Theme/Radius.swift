import Foundation

/// Corner radii from Finalist B specimen cards and mobile tiles (`12` / `16` / `10`).
enum Radius {
    static let none: CGFloat = 0
    static let pack: CGFloat = 4
    static let tile: CGFloat = 10
    static let object: CGFloat = 12
    static let panel: CGFloat = 16
}
